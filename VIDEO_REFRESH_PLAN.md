# 3D Ledger 설명 영상 v2 제작안

상태: **사전 제작 진행 중 / 최종 화면 녹화 보류**

기존 6분 5초 영상은 `replay.html` 중심의 이전 설명판이다. 새 영상은 최근 추가된 Scenario Lab, B200 배치 검증, 캐시 적중·미스·교체·되쓰기, 기존 L2 분할과 추가 3D SRAM의 구분을 메인 흐름으로 삼는다.

## 제작 순서

### 지금 진행하는 일

- 최신 기능을 기준으로 이야기 순서와 내레이션을 먼저 확정한다.
- 화면을 짧은 독립 챕터로 나눠 UI 일부가 바뀌어도 해당 챕터만 다시 찍을 수 있게 한다.
- 실측, 구조식에서 유도한 값, 설명용 시뮬레이션, 아직 없는 3D SRAM 가정을 음성과 화면에서 같은 말로 구분한다.
- 숫자와 버튼 이름은 최종 녹화 직전 현재 코드·원자료와 다시 대조한다.

### 화면이 동결된 뒤 하는 일

1. 16:9, 1920×1080 기준으로 브라우저 화면을 최종 녹화한다.
2. 확정 대본으로 한국어 음성을 녹음하고 장면에 맞춘다.
3. 한국어 VTT 자막과 포스터를 새로 만든다.
4. MP4·VTT·포스터를 페이지에 연결하고 파일 지문을 갱신한다.
5. 데스크톱과 모바일 폭에서 재생·자막·링크를 확인한다.

## 새 영상의 핵심 메시지

> Ledger3D는 “3D SRAM이 얼마나 큰가”만 보여 주는 화면이 아니라, weight·KV·state가 기존 캐시 공간을 놓고 경쟁할 때 어떤 바이트를 남겨야 실제 off-chip 이동을 줄이는지 비교하는 원장이다.

다음 네 경계를 영상에서 반드시 지킨다.

- B200 persisting-L2 결과는 **배치 정책의 대리 실험**이며 제작된 3D SRAM 실측이 아니다.
- Scenario Lab의 타일 이동은 **설명용 LRU/write-back 시뮬레이션**이며 실제 GPU cache-line trace가 아니다.
- `1.1099×`는 동일 64 MiB 객체 통제 실험의 결과이며 end-to-end LLM 속도 향상이 아니다.
- 추가 3D SRAM과 기존 L2를 잘라 보호 영역으로 쓰는 경우는 opportunity cost가 다르므로 같은 결과로 합치지 않는다.

## 목표 길이와 장면표

| 구간 | 화면 | 전달할 내용 | 변경 민감도 |
|---|---|---|---|
| 0:00–0:25 | Scenario Lab 첫 화면 | 무엇을 담고 어디로 움직이는지, 새 영상의 질문 | 낮음 |
| 0:25–1:05 | 전체 시스템 · 짧은/긴 문맥 | weight·KV·state의 저장량과 스텝 이동량은 다름 | 중간 |
| 1:05–1:50 | 하이브리드·슬라이딩·MoE·MLA 프리셋 | 모델 이름이 아니라 객체의 수명·읽기·쓰기·공유로 판단 | 중간 |
| 1:50–2:35 | 추가 SRAM ↔ 기존 L2 분할, capacity cliff | 자리를 남겨도 기존 캐시를 너무 많이 빼앗으면 손해 | 높음 |
| 2:35–3:50 | 한 요청 따라가기: cold → reuse → dirty | L2 확인, 미스, HBM 읽기, 적중, dirty eviction과 되쓰기 | 높음 |
| 3:50–4:45 | B200 배치 검증과 원자료 링크 | 동일 64 MiB 실험, 18조건·1,620 trial, 실패 구간도 함께 제시 | 중간 |
| 4:45–5:25 | `replay.html` B200 기록 재생 | 실제 시간축과 구조 계산을 섞지 않는 기존 원장의 역할 | 중간 |
| 5:25–6:00 | 근거·한계 화면 | 측정/유도/가정 구분, 아직 주장하지 않는 것 | 낮음 |

예상 완성 길이: **약 6분**. 각 구간은 독립 클립으로 녹화한다.

## 최종 녹화 동결 조건

아래가 모두 충족되기 전에는 최종 MP4를 만들지 않는다.

- `scenario-lab.html`, `replay.html`, `index.html`, `memory.html`의 정보 구조와 버튼 이름이 확정됨.
- B200 배치 요약 수치와 주장 문구가 `RESEARCH_CLAIMS.md` 및 공개 JSON과 일치함.
- Scenario Lab 자동 검사와 브라우저 확인이 통과하고 콘솔 오류가 없음.
- 설명용 가정의 기본값과 단위가 확정됨.
- 최종 녹화 기준 커밋을 하나 정해 화면·음성·자막이 같은 버전을 가리킴.

## 최종 숫자 확인표

녹화 직전에 원자료에서 다시 확인한다.

- 동일 64 MiB 객체, 두 B200 통합 기하평균: weight `1.0554×`, KV `1.0562×`, state `1.1099×`.
- state가 장치×순서×반복 셀에서 1위: `36/36`.
- 크기·layer-slice sweep: `18`조건, `1,620` trial, validation 오류 `0`.
- B200 장치 조회 L2: `132,644,864 B`.
- persisting reservation 최대: `82,903,040 B`.

수치가 바뀌면 대본, 자막, 화면 카드, 포스터 문구를 한꺼번에 갱신한다.

## 산출물

- `assets/b200_walkthrough_v2.mp4`
- `assets/b200_walkthrough_v2.ko.vtt`
- `assets/b200_walkthrough_v2_poster.png`
- `assets/b200_walkthrough_narration_v2.ko.txt`
- 페이지의 영상 설명과 캐시 버전 문자열
- `MANIFEST.sha256`

기존 MP4/VTT/포스터는 새 버전 검증이 끝날 때까지 보존한다. 새 파일 연결과 회귀 확인이 끝난 뒤에만 기본 영상으로 교체한다.
